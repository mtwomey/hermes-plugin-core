---
name: {{PLUGIN_KEY}}
description: "{{PLUGIN_NAME}} tools: ping and more."
version: 0.1.0
author: Your Name
license: MIT
platforms: [macos]
triggers:
  - "{{PLUGIN_KEY}}"
  - "{{PLUGIN_NAME}}"
---

# {{PLUGIN_NAME}} Plugin

Provides tools for interacting with {{PLUGIN_NAME}}.

## Setup

```bash
./setup.sh install
```

## Available Tools

### `{{PLUGIN_KEY}}_ping`
Test connectivity and authentication.

## Credentials

| Key | Description |
|-----|-------------|
| `api_key` | API key for {{PLUGIN_NAME}} |

## Common Patterns

```python
# Test connectivity
{{PLUGIN_KEY}}_ping()
```

## Pitfalls

- **Credentials missing** — run `python setup.py credentials configure` to store them.
- **Plugin not appearing** — verify `{{PLUGIN_KEY}}` in `plugins.enabled` in config.yaml; check symlink; restart Hermes.

## Notes

TODO: Add usage notes.
